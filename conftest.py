"""
conftest جذر المشروع — يُحمَّل قبل أن يستورد pytest-django الإعدادات.

⚠️ الغرض الوحيد: التصريح بأن بيئة الاختبارات هي "dev".

   DJANGO_ENV بلا default في config/settings/__init__.py عمدًا (راجع
   docstring هناك): النشر بلا متغيرات بيئة كان يحمّل إعدادات التطوير
   بصمت بدل أن يسقط برسالة مفهومة. وبما أن الاحتياط الضمني أُزيل،
   تُصرّح بيئة الاختبارات عن نفسها هنا صراحةً.

   setdefault لا override: لو شغّل أحدهم الاختبارات بـDJANGO_ENV مضبوطًا
   مسبقًا (بيئة CI مثلًا)، تُحترم قيمته ولا تُدهس.
"""

import os

os.environ.setdefault("DJANGO_ENV", "dev")
